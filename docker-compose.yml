services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: scheduleassist
      POSTGRES_PASSWORD: scheduleassist
      POSTGRES_DB: scheduleassist
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U scheduleassist"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
